interface:
  display_name: "Platty MCP Spec From Figma"
  short_description: "Create or augment product SDD from Figma evidence"
  default_prompt: "Use $platty-mcp-sdd-spec-from-figma to auto-select CREATE or AUGMENT for a Figma-backed product plan or 기획서 request: default to accuracy-first, use $platty-mcp-figma-design-sync, finish one ScreenIntentSeed per major screen before retrieval, route all seeds through $platty-mcp-retrieval, create one EpicScreenSpecPool collection lane per selected EPIC and Design map instead of one worker per screen, account for the complete linked screen_spec union before per-screen matching and source closure, keep final classification and canonical writes with the coordinator and $platty-mcp-sdd-spec, and persist prd.md, user_stories.md, and validated figma_handoff.json without guessing unresolved current facts or starting technical design."
