# Build EPICs Core

Deterministic helpers shared by `build_epics_cli_runtime`.

This module contains validation, document-index loading, editable-draft helpers,
and confirmed-plan persistence. It intentionally does not export the legacy
`runBuildEpics` LLM orchestration path.
